# Phase 5 integration review

Baseline: `c0331dd2` (completed Phase 4 checkpoint). Scope: the source checkpoint containing this report; exact working-file and Git blob hashes are retained in the evidence manifest. Generated indexes are committed separately. This is a bounded review of the new case projection/UI, refresh audience and operation-list reads, not a release-wide security claim.

## Method and boundaries

The repository security policy and the pinned methods recorded in [Phase 4](review-phase4.md) apply: access-control/execution traces from pashov, state/accounting and verified hypotheses from Plamen, and trust-boundary/specification/invariant/false-positive review from Trail of Bits. Independent agents cross-reviewed code they did not implement; root reviewed integration and exercised the real browser. These are JavaScript/PostgreSQL adaptations, not claims of upstream orchestration or a Solidity audit.

The selected mystery is planned before the aggregate's one authenticated knowledge union. Read-scope identity and viewer branding guard its renderer. Current account/character ownership is checked before a board is projected. Knowledge changes cannot split a repeatable read snapshot; the next read sees revocation. DTOs never authorize writes. Existing mystery commands still validate current prerequisites, exact definitions, inventory and replay receipts. The UI uses only issued actions, escapes text, and carries no new reward or privileged write authority.

## Findings and disposition

- **P5-LIVE-01 (integration):** the notifier's pilot-only default object list omitted new public Furnace consequences. It now uses the same admitted definitions as the routes. Tests preserve durable-event checks, hidden actor-only hints, public cohort fanout, disabled content and active-account filtering on memory and PostgreSQL.
- **P5-PERF-01 (read amplification):** the Family list and graph-reference list used an OR over current membership, opener and historical participation. Despite small result limits, the native Family query scanned 50,001 operation rows and 50,000 commitments in a fixture with one visible operation (2,677 shared blocks; 23.72 ms on this local run). Bounded ordered authorization branches now use UNION with final deduplication/order/limit. Two indexes cover opener and account commitments; existing membership/role indexes are reused. The same fixture reads 23 buffers and three indexed operation rows, returns the same single row, and visits no commitment rows. A larger 50,000-unrelated-row fixture verifies all branches before/after membership removal, duplicate promises, exact result order/truncation and scan-row bounds, including index scans. An initially rejected merge plan is retained in the evidence. Elapsed times are observations, not test assertions.
- **P5-UX-01 (clarity):** completed notes displayed an obsolete explicit-interaction requirement. Terminal statuses now hide obsolete blockers, with a renderer regression. Catalog fallback titles are readable words without modifying authored identities or definition hashes.
- **P5-UI-ISOLATION-01 (character binding):** old-character to no-character to heir transitions retained old selections because the intermediate 404 discarded identity before cleanup. Losing a known character now clears selections, pending retries and confirmations. Focused tests prove a bare World reload for the heir while preserving bootstrap retry recovery until the first character is known. Independent review verified the correction.

## Executed evidence

New memory/native composition proofs check one REPEATABLE READ READ ONLY transaction, no writes/row locks, one source-authentication pass, combined operation/recipe/world/mystery prerequisites, concurrent revocation, next-read denial, owner/heir isolation and preserved earned prose. API tests check default-off/cohort/authentication, closed query inputs, unknown selections and descriptions. Existing mystery/projection/client checks pass.

The browser runs the real app with an isolated database: start case, read clue, complete a server-issued action, simulate losing its successful response, reload and retry the exact saved key/body, and verify one stored completion. Hidden clue text stays absent. Desktop and 390-pixel mobile views have no uncaught JavaScript errors or horizontal overflow; keyboard refresh works. Focused client tests cover stale selection responses, confirmation cancellation on invalidation, irreversible choice retries, restored request binding, escaped content and omission of unissued controls.

The final nine-suite memory/API/client projection gate and five-suite native projection gate pass. Existing Family/query regressions and populated repeated migration pass. A fresh final full-server PostgreSQL16 check passes 203/0. All 3,957 static SQL statements prepare; the separate interpolation guard remains red at 189 versus its unchanged ceiling of 168. Documentation, syntax and development preflight pass. The completed Phase4 checkpoint also passed the entire pretest chain (including 100 seeds/25,000 property actions); its 121-command main chain has 119 passes including separately completed knowledge provenance, and the same two baseline failures in routes/gates. Phase5 reruns affected checks rather than presenting those prior broad results as a new all-suite pass.

## Remaining limits

The measured recipe preview is bounded but performs up to 260 reads for a synthetic 50-recipe catalog with four caps per recipe; the shipped composition has two recipes. Family readiness is bounded by its participant limit but still has per-participant reads. These are documented scaling limits; no cross-request authorization cache was introduced. Full-key scarcity lookups already use the primary-key index. A future larger admitted catalog should batch read-only cap/inventory lookups and measure again.

Process-local refresh hints have no durable outbox. Content and feature flags remain off by default. Existing repository release failures and interpolation debt remain explicit. No production activation, push, deployment, migration against production, signer change or OMR transfer occurred. These read/UI changes do not require Solidity callback or transfer testing.
