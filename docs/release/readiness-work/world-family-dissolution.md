# Scoped shared Family dissolution custody

This test-only increment starts from `361efd9079837d4ac2bcff796b0c7c7cffeb6b7e`. It extends the shared resource observer's ordinary Family entry subset with one concrete terminal: a single Family disappears through voluntary living-member departure. It reuses the cash/ammo journal's disposal equations and the Family OMR journal's exact reserve-to-desk recycling equation. Production code, shared native proof infrastructure and worker scheduling are unchanged.

For each nonzero treasury/reserve/ammo bucket, exactly one fresh `gang:dissolved` row must carry the full negative amount, that Family counterparty, and null personal owners. Cash and ammo are destroyed. **OMR is transferred to the desk**, with exactly one positive `desk:recycle` receipt for `gang:dissolved`, exact desk balance and lifetime-input increments, unchanged desk sale/buy counters, and zero unexplained movement at every other observed OMR owner. No token burn is inferred from the legacy reason name. Family and membership disappearance are mandatory; unrelated Family custody must stay unchanged. Every match is one-use; stale or duplicated receipts cannot authorize disposition.

The shared observer proves receipt/custody lineage. The native test separately binds completed canonical HTTP requests to durable account/key, route/body hash and response, and requires all original departing members' actual leave commands with exactly one dissolved response. It retains a concurrent duplicate as a bounded invocation/completion batch, without claiming total PostgreSQL commit order. A replay must return the same response with no new movement. Multiple-Family deletions, death/estate, other compound receipts, and nonmonetary turf/war/territory/governance cleanup are explicitly outside this classifier. The existing unknown-row diagnostics remain intact. Full-resource and release qualification remain false.

```powershell
node test/rc1-world-family-dissolution.js
$env:RC1_RESOURCE_DATABASE_URL='postgres://postgres@127.0.0.1:55438/postgres'
node test/rc1-native-family-dissolution.js --postgres --output=<fresh-private-directory>
node test/rc1-world-family-dissolution.js --evidence=<fresh-private-directory>
node test/rc1-world-family-entry.js
node test/rc1-world-resource-observer.js --postgres
```

The native runner owns a unique disposable local database. Before canonical preparation it declares four ordinary fixture accounts with respect eligibility, default cash/ammo, 1,000 OMR reallocated from the retired AMM seed, and a local exchange till. Canonical cash-window redemptions, Family formation/join and cash/OMR tributes are retained. The existing canonical GTA→melt pattern supplies nonzero Family ammo and cash, with at most 20 attempts per founder and a six-hour logical preparation bound. Own-character public cooldown/jail/energy information drives waits; every due original worker callback executes under the existing shared application/SQL clock. No car, ammo or membership is injected. Acquisition and other preparation changes keep their actual shared unknowns. There is no personal ammo-tribute endpoint.

After the declared terminal baseline, the first boss departs and custody survives succession. The remaining member's leave is interrupted by a diagnostic trigger at `DELETE gangs`, after all three disposal receipts and the OMR recycle/desk writes. The trigger asserts those predecessors before aborting. Full observed custody and receipts must roll back. Removing the trigger allows the exact same key to succeed and replay without value. The second Family tests concurrent same-key last departure and durable replay. A final 15 minutes of every due original callback is retained in addition to acquisition waits. No expiry boundary is needed for voluntary dissolution; no unrelated lifecycle credit is claimed.

The first clean native run at `bd038f259cd4576e079ecbe81abddb88191d5588` is retained as **FAIL** (`family-terminal-bd038f25-native-1`): 29 boundaries completed before the late-abort full-snapshot assertion detected prior due accrual. Its original canonical GTA preparation took one and four attempts, with eight Family rounds from each melt and 15 minutes of original timers. The failed action correctly preserved all cash/OMR/ammo custody, membership and receipts; only energy, nerve, accrual credit clocks and `last_accrued_at` changed. `src/game.js:settleIfDue` deliberately commits elapsed accrual in a separate transaction before the action. This diagnostic precondition error is corrected by an observed canonical member read at the same logical instant before installing the fault. The full-state rollback equality is kept strict; no production behavior or clock is changed. No successful terminal is claimed for the failed run.

Pure and captured-native corruption controls cover missing/stale/duplicate disposal and recycle receipts; incorrect exact amounts, personal owner and Family counterparty; missing desk lifetime credit; balanced reserve diversion to a different account; retained Family/member custody; and missing, duplicated or incorrectly bound durable requests. Inputs, failures, first failing state, original worker schedule, random tape and source hashes are private rerunnable artifacts. The public summary contains only source, counts, hashes and exclusions.

Security review uses the repository policy's pinned Pashov `c577eb7799c349de0acb187ba00ca98e14e436fd`, Plamen `795962b96e254f2e423a2635fe7f8cb8ea1e6d69` and Trail of Bits `d3323cefbcf645678b8dc481de204b02ad3d02dc` methods through direct authority/caller inspection, per-owner invariants, adversarial receipt/rollback controls and independent artifact replay. Authorities inspected are `removeMember`/`leaveGang` in `src/social/gangs.js`, recycling in `src/game.js:ledger`, `src/economy.js:boostCar`/`meltCar`, server idempotency hooks and the Family/desk schema. No Solidity, chain/provider broadcast, production backing or whole upstream orchestration claim is made. Existing historical component passes are references, not inherited execution coverage for this source.
