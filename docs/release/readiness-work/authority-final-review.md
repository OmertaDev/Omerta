# RC1 backend authority review, 2026-09-24

The backend authority review is complete for application source
`357ff20a38316bebfc2a9d8ae4413f505bca8aa6`, within the scope and evidence distinctions
below. This is a code review of all mounted mutation rails, not a claim that any
production rail is disabled, configured, funded, or authorized for activation.
The [review and route matrix](authority-final-review.json) pins the reviewed
files, retained inputs, findings, assertion references and private evidence hashes.

The source contains 529 mounted mutation routes: 379 current-character, 37
two-character, 60 account/domain, 42 moderator, four reviewer and seven explicitly
public routes. Each route is mapped to its actual principal boundary, canonical
callee and retained test references. The command matrix covers eight families,
23 currently emitted types and the historical `operation.assign` type, whose
direct API is reviewed but whose PlayerCommand projection does not emit it.

The earlier route matcher missed dynamic URLs, object-form requests, explicit
social-module reexports and permanent portfolio tombstones. These are resolved
with 24 explicit supplementary assertion groups. References to a call are
navigational evidence; the review separately records the actual shared guards,
domain predicates, source equivalence and executed assertion scope. It does not
infer authority coverage from an anonymous 401 or a passing suite total.

## New native proof

The existing residual harness ran on clean source `357ff20a`, Node 24.19.0 and
PostgreSQL 18.4 in its own loopback database. It passed six canonical mutations:
cancel own search, repair own car, cancel loading convoy, unlist race car, unlist
speakeasy and leave/disband raid. Its 23 denials preserve all 368 measured authority
tables, and eight exact retries return their stored result without new effects.
Positive assertions cover caller isolation, server-derived repair charges and
ledger entries, cargo conservation, member departure and leader disbanding.

The independent verifier checked the four indexed artifacts, complete history
hash chain, every denial/replay state comparison and absence of test-owned
schemas. Command exit was zero. This is fixture-assisted evidence; passive
accrual is frozen before measured actions. The outer HTTP receipt table is
checked through replay identity/body semantics, not included in denial snapshots.

## Review and reused evidence

The review follows the repository policy's pinned Pashov access-control traces,
Plamen state/outcome checks and Trail of Bits context/invariant/false-positive
methods. It reuses the nine source-applicable authority suites and existing
native command/domain, capital and resource proofs. The new work adds one
bounded native harness execution and evidence verification; it does not claim
an upstream orchestration or a new stateful fuzz campaign.

All retained route-file hashes, 239 aggregate test hashes and 52 command review
file hashes matched this checkout. Application Git content also matches root
revision `92e50fb9`; the root checkout's `schema.sql` has LF line endings while
this checkout has CRLF. The manifest preserves the real byte hashes rather than
silently calling them identical.

The finite prior questions have individual dispositions in the JSON review:

- Both Furnace choice outcomes have native assertions. Opposite-choice
  serialization follows the same locked mystery row before the stored choice
  check; a simultaneous opposite-choice schedule was not newly run.
- Native operations test approved-state Knowledge revocation and membership
  departure, occupied seats, leave/rejoin, historical deposits, terminal refunds,
  competing execution and injected rollback. Join/assign and cancel/execute
  share the reviewed operation lock, changed-union check and terminal refusal.
- Positive service expiry and short authored wall-clock expiry are reused with
  the thin, authenticated HTTP dispatcher. This is an explicit source-equivalence
  conclusion, not an executed positive HTTP expiry or natural 24-hour journey.
- All five salvage exclusion flags have explicit sequential assertions, with
  separate native custody/locking/replay evidence. The flags are independent
  refusals, so combinations introduce no new accepting branch.
- Moderator credentials and privileged payload behavior are separately mapped.
  The reviewer is a global role, so another account's nomination is an intended
  target; reviewer identity and proposal lifecycle remain guarded. Provider,
  signer and transaction seams prove code behavior, not live external identity.

The only application changes since the hosted baseline are the persisted
resident turn and deterministic jailbird selection. The review follows selection,
character/state lock order, atomic pending removal and caught-failure recovery.
Retained native interrupted/recovered proofs and their artifact/history checks
support this delta. They do not replace the separate whole-worker duration gates.

## Findings and limits

The medium moderator parser-ingress finding is fixed and retested. Invalidated
command/Crew harness expectations, the pending-409 harness lifecycle finding and
the passive-accrual snapshot mistake retain their original failure records and
later dispositions. No confirmed critical/high backend finding remains in this
reviewed scope. The new residual tests repaired an evidence gap and changed no
production behavior.

The joined source-phase measurements are zero uncovered routes, zero uncovered
command families, zero unresolved confirmed critical/high findings, a complete
applicable role/command matrix, and completed security-policy review. The
[contract/external-rail applicability review](final-contract-review-reuse.md) is
joined by content hash, including all 59 production contracts, retained static
dispositions and current-source comparison. Its medium M-02 activation condition,
CORE-CFG-01 configuration condition and low ORACLE-SOURCE-HORIZON-001 limitation
remain explicit; test totals do not close them.

The conclusions apply to the declared source/local qualification phase and to
root candidate `40ef4e83`, whose application/contract Git content is identical.
Deployment configuration, activation conditions, physical devices and the
existing duration/resource qualification gates remain separate. Future changes
to the pinned authority code or authored requirements reopen the affected proof.
