# TOOL47: City worker session-lock release

Owner: Codex/native_harness. RC1 release-retest scope: `src/bank.js::runCityLeg` session acquisition and cleanup only. This runtime correction is separate from the serialization-only commit `74b2918023d14f442138b3cf982943647b675e99` and the Family classifier TOOL46 correction.

The frozen74 worker run retained a real observer rejection at the NPC Family-creation commit. Later original worker jobs encountered that same retained observer error. `runCityLeg` acquired a connection and awaited its session-lock query **before** entering its release `try/finally`. The observer rejected after PostgreSQL returned success, leaving a checked-out client holding advisory lock class16971. The actual worker controller then waited indefinitely in `pool.end()`. The original failure/checkpoint artifacts and declared watchdog are preserved; this is not a completed or qualifying world run.

A separate owned-PostgreSQL causal probe at the unchanged74 source called the actual City function and commit observer. It retained exactly one successful native lock SELECT followed by the injected observer rejection, zero release calls, pool total1/idle0, the held lock, and actual controller.close still pending. Destroying only that probe-owned leased client made shutdown complete and allowed its exclusively owned database to drop. This demonstrates an instrumentation-triggered rejection; it does not assert that the same production incident was observed. Probe: `C:/Users/Jorge/.codex/rc1-readiness-private-20260921/observer-serialization-74b29180-1/lease-causal-result.json`.

The repair encloses acquisition in the existing release guarantee. Known lock=false releases once without unlocking. Known lock=true retains original work, unlock and release. An acquisition rejection has an unknown result: the session might already hold the lock, so that checked-out client is discarded instead of returned to the pool. Unlock rejection also discards the uncertain session. Original work/query error identity is preserved; no pool-wide termination, timer, economic calculation, SQL statement or accounting change is introduced.

The seven pure controls execute the actual extracted acquisition wrapper against a stub inner job. Native controls use an owned database initialized by original makeDb and the canonical empty City leg, then exercise pre-query error, actual termination of the probe's checked-out PostgreSQL backend before acquisition, post-success observer rejection, another session holding the lock, normal empty success, original inner rollback, and unlock rejection before/after native completion. They require each lock-client release exactly once, only known acquired locks explicitly unlocked, ambiguous sessions discarded, complete canonical state unchanged, actual worker pool-close completion, no remaining advisory lock and database cleanup. The native transport control is a controlled local fault, not a production incident.

The security review follows the repository's retained trace, trust-boundary and recovery methods: checked-out PostgreSQL sessions are the lock authority; callbacks can reject after a native side effect; custody must not return an uncertain lock session to the shared pool. Scope excludes payout arithmetic and other keepers. Existing Bank accounting tests and a new source-specific original-worker observation/replay remain required affected checks. No contract/chain surface changed, so Solidity/signing/recipient-fuzzing claims are inapplicable. No broad static-analysis or repository-wide recovery claim is made.

Reproduce pure controls with `node test/rc1-city-lock-release.js`. For native controls, set disposable local `COORDINATION_TEST_DATABASE_URL` and run `node test/rc1-city-lock-release.js --postgres --output=<new-restricted-directory>` from a clean committed checkout. Final source-bound results and original failure identities are retained separately.
