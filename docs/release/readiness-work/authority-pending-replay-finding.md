# TOOL53: concurrent HTTP harness must retain pending denial and drain calls

Candidate `b5c80a4ed921e58792e78f8c4b51de5635182b74` failed its Crew authority gate after 51 denials. The second same-key request correctly returned HTTP409 `in_progress` while the first owned a durable reservation. The test required two immediate200 responses, although `src/server.js` explicitly returns409 for an incomplete legacy HTTP receipt. Earlier source64c3245a and Family component0049b92a happened to observe a completed receipt and two200 responses; they do not cover the pending schedule.

The mistaken assertion also allowed Promise.all to reject before the first HTTP call completed. Teardown dropped the test's isolated schema while postcommit hooks/receipt storage still ran, producing retained42P01 diagnostics. This is a test-lifecycle failure, not an observed production schema loss or a demonstrated duplicate gameplay effect. Original artifacts and logs remain unchanged; the failed run never qualifies.

The repair holds the genuinely reserved request at a test-only preHandler barrier, before its canonical handler. Its exact concurrent retry must return409 without changing any authority table. The test then releases the original request, requires one fresh200 result, and requires its next retry to return the exact stored acknowledgment with no authoritative write. Every outstanding HTTP call drains before application/schema cleanup on both passing and failing paths. Runtime behavior, idempotency identity and denial requirements are unchanged. Both Crew and the new Family component receive the same correction.

Fresh native PostgreSQL 18.4 retests at clean source `4ad7dbcd87a2d524cead2d2647a88b06f4c89443` passed: Crew retained 53 denials, 14 replay comparisons and 90 responses; Family retained 51 denials, 15 replay comparisons and 89 responses. Each run retained 68 indexed artifacts. Independent full-byte/history checks and a separate contention audit established one fresh kick, one unchanged-state pending409, two exact cached acknowledgments and only the observed target's removal. Family also retained changed-payload422 with unchanged authority. Application/schema cleanup completed; a subsequent prefix census found no test schemas. No creation-time per-run schema/OID receipt was retained, so this is not an independent per-OID cleanup attestation.

Candidate17's original local failure remains FAIL. Both Linux hosted legacy-authority jobs at that same b5 source also failed with the same explicit pending409-versus200 assertion: workflow `35598247604`, PostgreSQL16 job `106328180827` and PostgreSQL18.4 job `106328180833`. Their complete private logs are retained; no hosted cleanup-cause inference is made from the local trace. The source4ad retests do not clear those failed jobs or the integrated candidate. Fresh integrated and hosted execution remains required.

The [source-bound results](authority-family-results.json) and [retention index](authority-family-retention-index.json) bind the failed run, original Family component, repaired runs, independent audits and hosted failure logs. This closes the reproduced test assertion/lifecycle defect within these two components. Full authority coverage, arbitrary revocation schedules and RC1 release readiness remain open.
