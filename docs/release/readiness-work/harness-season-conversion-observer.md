# Receipt-bound season conversion evidence

Owner: Codex/native_harness. Base: `1404b48c30b41ca66b757eb2c2b39fc6e2750553`. Observer/test changes only; canonical worker, timing, rules, history capture and actor policy are unchanged.

The retained quiet world at `0648d1bda630b596d6eeb1bfbd76a55132e294b5` had 45 unsupported occurrences: 29 `season_recaps`, 2 `season_records`, 6 car acquisition, 2 NPC formation, 2 `gang:found` receipts, 2 Family lineage and 2 membership changes. This component addresses only the 29 recap/conversion boundaries. Four have positive prestige gains totaling 24; 25 level-one recaps have zero gain and qualify only as status metadata. The original run and its classification remain unchanged.

`src/worker.js::runSeasonRollover` is the authority: each living character is locked separately; canonical `levelOf(respect)` determines `floor(level/2)` prestige; `recapTitleOf` selects the title; respect, seasonal kills and Elo reset; the account's seasonal spend counter resets; an account-bound `season_convert` telemetry receipt and unique `(account_id, season)` recap are written in the same transaction. The observer imports the existing rules and requires the exact receipt payload, account ownership, unique eligible character, timestamps, recap fields, reset values and positive prestige delta. It rejects duplicate/reused receipts, edited/deleted recaps and altered retained conversion receipts. No unexplained grant is inferred from balance changes.

Only a single fresh conversion with no extra table, account, character or telemetry effects is classified. Duel-title awards, multiple/existing-recap conversions, ambiguous character identity, other compound effects and `season_records`/crowns remain explicit unknowns. Canonical deletion of old telemetry in a separate pruning boundary is outside this classifier; its original acknowledged receipt remains in evidence history. This does not claim permanent telemetry retention or validation of the pruning rule. Zero-gain conversions add no economic grant checks.

`node test/rc1-season-conversion-journal.js` includes a shared-observer integration control, 32 rejecting corruption controls and 7 explicit unsupported cases. `--retained=<0648-observe-directory>` first verifies every original artifact and complete history, then checks the four exact positive changed-row projections (24 prestige) and 16 corrupted projections. Those projections do not contain every unchanged row and are not a substitute for full-world uniqueness or native execution. The first development fixture omitted required OMR fields and failed the shared observer's existing numeric guard; supplying the complete fixture fixed the test without changing the observer guard.

The predeclared native retest is one fresh PostgreSQL original-worker quiet-world observation and recorded replay: 2 logical hours, 25 actors, seed `rc1-alpha`, resources enabled, owned databases, gzip event-member capture. Limits remain 1,200,000ms / 536,870,912 output bytes / 536,870,912 free-byte floor per world; gzip decoded/stored/line limits are 536,870,912 / 134,217,728 / 8,388,608 bytes, with 64 pending/outstanding records. An external 25-minute watchdog records failure/incomplete, never success. Required comparisons are all 18 actor-replay fields, full initial/final canonical tables and sequences, complete resource journal bytes and artifact/history verification. Native positive and zero-only conversions are counted separately. No bootstrap conversion, two-hour run or observer classification closes applicable seasonal duration, all resource classes or any matrix cell.
