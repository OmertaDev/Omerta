[db] Postgres ready — column migration ran 2956 ADD COLUMN IF NOT EXISTS statements.

pgquery — 4061 static statements prepared against real Postgres
  168 interpolated (cannot be prepared — counted, never silently passed; PGQUERY_LIST=1 to list)
  557 non-DML (DDL / transaction control — not preparable by design)
  39 call sites whose argument is not a literal (a variable or a helper)

✅ pgquery passed — every static SQL string in src/ parses and type-resolves on real Postgres.
