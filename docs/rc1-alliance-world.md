# Coordinated alliance in the native world runner

This adapter extends `test/rc1-native-world-workload.js` with `--policy=coordinated_alliance`. It is a distinct 25-actor schedule, not the quiet world's rounded 10% roster. Use `--hours=48 --population=25 --seed=rc1-alpha --observe-resources`; an exact replay additionally uses `--replay=<sealed observation directory>` from the **same clean source**. The local control database and fresh restricted output directory remain mandatory. Package/workflow/registry integration is outside this change.

All 25 accounts and characters enter through the ordinary guest and character HTTP routes. Only founders 0–2 receive initialization respect for level 75. The 22 outsiders receive no progression, cash, item, membership or permission grants. After the measured baseline and original-worker bootstrap, founders fund actual formation through canonical check-ins, form three Families, make three bilateral pacts, travel as needed and independently discover docks/foundry/docks Split Ledger sources. Every founder contributes explicit account-granted evidence. Pacts do not grant Knowledge ACLs; cross-Family operation execution is not supported.

All 25 actors receive one eligible canonical crime session at hour 0 and hour 24. The contributors conclude their investigations at hour 24, after the intervening actual seasonal rollover. Original hourly, season, Director and health callbacks run throughout the full 48 hours with their authored durations. Pacts and shared evidence are checked again through their current authorized views. Pact-only and outsider claim reads must return the authored generic 404. Share and conclusion retries must preserve the complete canonical database snapshot.

The adapter accepts actor projections only. SQL is limited to declared initialization fixtures and separate evidence observers. Each selected request retains its exact opaque target/action, revision and idempotency key in the original component checkpoint and adapter checkpoint, both restored before dispatch. The actor tape verifies exact authorized inputs and responses. The adapter checkpoint enters the existing policy-state hash; full canonical state, RNG, query selection, worker outcomes, actor tape, diagnostics and the complete committed-boundary resource stream retain their existing equality checks. There are no new comparison exclusions. Resource drift fails; unsupported resource classifications remain explicit and do not become passes.

Process-level database continuation (`--resume`) fails closed for this adapter. Request/checkpoint restore and exact fresh-world replay do not imply interrupted-process continuation coverage. Only the alliance HTTP calls use authentication; ordinary daily crimes retain the runner's canonical domain API. Local rate/invite/social gates are disabled as declared, so this run does not qualify admission, external auth, capacity or production latency. Full resource taxonomy, 90 days, other seeds/populations, the matrix and deployed participants remain unqualified.

Source ancestry starts at `9e05bf089fff68ba6c65b805ac6b1c5c33c26c24`, with prior Family custody, alliance and turf components retained through `73c46cc7af5e14cee1bfaed08b5c71446a08c429`. Native results will be recorded after the immutable observation/replay pair completes.
