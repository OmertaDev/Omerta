  ✓ all 777 statements in schema.sql are re-apply safe (the file is applied at every boot)
✅ Schema-integrity test passed — MED-1: 2771 idempotent ADD COLUMN IF NOT EXISTS statements derived from schema.sql (no leakage, clean no-op on a fresh DB, a dropped later-added column is RE-ADDED). MED-2: all 89 character-scoped tables (character_id OR a named %_character role) have a documented death disposition (57 wiped / 17 special / 7 escrow / 7 ledger — a new unclassified table fails CI closed, and every wiped/special table has a DELETE or a resolving status UPDATE in src). Phase 1: 12 world-graph tables and 8 generic owner/depositor tuple roles have explicit fail-closed no-inheritance dispositions.
