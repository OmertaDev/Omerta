# The audit reports — an index, and a warning about reading them

There are 95 of these, 15192 lines in total. Every one is a **point-in-time** report: it describes what was
true of the tree on the day it was written, including findings that were fixed hours later in the same
session. None of them is a description of the system as it stands.

**If you want to know how OMERTÀ works now, read `SPEC.md`.** If you want to know what the balance levers
are, read `BALANCE.md`; what is signed, `SIGN-OFF.md`; how to deploy, `DEPLOY.md` and `CHAIN-DEPLOY.md`.

These reports are kept because they are the only record of WHY a lot of the code looks the way it does —
a comment saying "AUDIT-full-system-v2 C-HIGH-1" is only meaningful with the report to read. Some 100
source comments cite one of these by name, which is also why they have not been moved into a
subdirectory: a citation that no longer resolves is worse than a cluttered root.

A finding described here as "flagged, not patched" may since have been applied — `SIGN-OFF.md` is the
record of what was decided, not this file.

| report | written | lines | subject |
|---|---|---|---|
| `AUDIT-casino-tables.md` | 2026-07-21 | 59 | AUDIT — Gambling Den step three (blackjack + heads-up hold'em) |
| `AUDIT-casino-tournament.md` | 2026-07-21 | 58 | AUDIT — Gambling Den step four (the poker tournament) |
| `AUDIT-chain-onchain.md` | 2026-07-18 | 161 | AUDIT — the chain rail, on-chain + interactions (max-effort, five-lens red-team) |
| `AUDIT-content-drops.md` | 2026-07-16 | 117 | AUDIT — the post-sign-off content drops (vendettas, crew heists, convoys, the Commission) |
| `AUDIT-contracts.md` | 2026-07-14 | 55 | OMERTÀ M6-A (Solidity) — Security Audit — 2026-07 |
| `AUDIT-convoy-npc-trucking.md` | 2026-07-22 | 57 | AUDIT — Convoy step three (NPC trucking) |
| `AUDIT-core-loop.md` | 2026-07-15 | 145 | OMERTÀ — Core Game-Loop Audit (design/experience lens) |
| `AUDIT-deep-deferred.md` | 2026-07-24 | 70 | AUDIT — the deep-system deferred four (Estate 2 · Commission 3 · Loan House · Ring+Bracket) |
| `AUDIT-five-pillars.md` | 2026-07-23 | 98 | AUDIT — The Five Pillars (honor · diplomacy · sovereignty · campaigns · bloodline) |
| `AUDIT-full-game.md` | 2026-07-15 | 194 | OMERTÀ — Full Game Audit (code · economy · loops · new-player) |
| `AUDIT-full-product.md` | 2026-07-23 | 120 | AUDIT — Full-Product Max-Effort Red-Team (2026-07-23 → overnight) |
| `AUDIT-full-surface.md` | 2026-07-20 | 139 | AUDIT — full-surface red-team (2026-07-20) |
| `AUDIT-full-system-v2.md` | 2026-07-21 | 152 | AUDIT-full-system-v2 — overnight max-effort full-system red-team (2026-07-21) |
| `AUDIT-full-system-v3.md` | 2026-07-21 | 99 | AUDIT — full-system red-team v3 |
| `AUDIT-full-system-v4.md` | 2026-07-21 | 124 | AUDIT-full-system-v4.md — full-system red-team (post-Futurity / post-Uprising) |
| `AUDIT-full-system.md` | 2026-07-17 | 104 | AUDIT — full-system max-effort red-team (M1 → M8 + Risk-to-Earn + chain) |
| `AUDIT-gameplay-chain.md` | 2026-07-14 | 128 | OMERTÀ Full Audit — gameplay loops, chain service, contracts, UX (2026-07-14) |
| `AUDIT-interactions.md` | 2026-07-15 | 90 | OMERTÀ — Interaction Audit (contracts × Risk-to-Earn features) |
| `AUDIT-law-world.md` | 2026-07-17 | 102 | AUDIT — The Law / RICO and The Living World (five-lens red-team) |
| `AUDIT-loan-sharking-step-three.md` | 2026-07-18 | 62 | AUDIT — Loan Sharking step three (the paper market) |
| `AUDIT-loan-sharking-step-two.md` | 2026-07-18 | 79 | AUDIT — Loan Sharking step two (secured credit & enforcement) |
| `AUDIT-loan-sharking.md` | 2026-07-18 | 118 | AUDIT — Loan Sharking (the Shylock), step one |
| `AUDIT-loan-wanted.md` | 2026-07-18 | 127 | AUDIT — Loan Sharking step four (WANTED) |
| `AUDIT-market-skills-underworld.md` | 2026-07-17 | 143 | Audit — Black Market, Skills, and the Underworld (steps 1–5) |
| `AUDIT-marriage-soldiers.md` | 2026-07-23 | 80 | AUDIT — Marriages & the Consigliere + Named Soldiers (founder picks #2+#3) |
| `AUDIT-megaproject.md` | 2026-07-23 | 48 | AUDIT — THE MEGAPROJECT (founder pick #1) — focused three-lens red-team |
| `AUDIT-population.md` | 2026-07-25 | 213 | AUDIT — THE POPULATION (steps one + two) |
| `AUDIT-port-step-four.md` | 2026-07-21 | 50 | AUDIT — The Port step four (the contraband market + berths) |
| `AUDIT-port-step-three.md` | 2026-07-21 | 54 | AUDIT — The Port step three (the Smuggler's Legend + the Harbormaster) |
| `AUDIT-port-step-two.md` | 2026-07-21 | 76 | AUDIT — The Port step two (naval upgrades + PIRACY + rendezvous) |
| `AUDIT-portfolio.md` | 2026-07-19 | 88 | AUDIT — The Portfolio ("Going Legit" / RWA) + all contract interactions |
| `AUDIT-redteam-loop.md` | 2026-07-22 | 2618 | AUDIT-redteam-loop.md — autonomous overnight red-team (rounds) |
| `AUDIT-rwa-float.md` | 2026-07-23 | 95 | AUDIT — THE FLOAT (the full-reserve RWA layer, R2 redesigned) |
| `AUDIT-secrets-collection.md` | 2026-07-23 | 118 | AUDIT — Blackmail & Secrets + The Collection (founder picks #7 + #8) |
| `AUDIT-session-drops-2.md` | 2026-07-21 | 99 | AUDIT — session drops v2 (Territory 3, Pen 4–5, faucet retunes) |
| `AUDIT-session-drops.md` | 2026-07-20 | 100 | AUDIT — session content-drops red-team (Boxing 3–5, Skills 2, Wire 2, World 2–3) |
| `AUDIT-session-races-convoy-vendetta.md` | 2026-07-22 | 61 | AUDIT — session drops (Street Races 2–3, Convoy 3, Vendettas 2) |
| `AUDIT-sim.md` | 2026-07-16 | 123 | OMERTÀ — Full Sim Audit (core loops, contract interactions, technical + economic bugs) |
| `AUDIT-skills-prestige-and-reroll.md` | 2026-07-21 | 121 | AUDIT — Skills step three (prestige carry) + randomized builds & the paid re-roll |
| `AUDIT-slate-drops.md` | 2026-07-24 | 86 | AUDIT — the slate trio: THE DUELING LADDER (#5) · CLUE SCROLLS (#4) · SEASONAL LEAGUE MODIFIERS (#6) |
| `AUDIT-stakes-spine-session.md` | 2026-07-24 | 113 | AUDIT — the stakes/spine session (L1/L2/L3 + the three entry verbs) |
| `AUDIT-street-races-step-two.md` | 2026-07-22 | 67 | AUDIT — Street Races step two (PINK SLIPS + NITROUS) |
| `AUDIT-street-races.md` | 2026-07-21 | 64 | AUDIT — Street Races (the new content drop) |
| `AUDIT-territory-racket-wars.md` | 2026-07-21 | 55 | AUDIT — Territory step four (fortification + rival raids) |
| `AUDIT-the-pen-step-two.md` | 2026-07-18 | 78 | AUDIT — The Pen step two (three-lens red-team) |
| `AUDIT-the-pen.md` | 2026-07-18 | 83 | AUDIT — The Pen (three-lens red-team) |
| `AUDIT-tier1-deepening.md` | 2026-07-24 | 75 | AUDIT — the Tier-1 → Tier-4 deepening program (6 systems) |
| `AUDIT-tier2-deepening.md` | 2026-07-24 | 87 | AUDIT — the Tier-2 → Tier-4 deepening program (4 systems) |
| `AUDIT-tier3-deepening.md` | 2026-07-24 | 127 | AUDIT — the Tier-3 → Tier-4 deepening program (combined red-team) |
| `AUDIT-track-stable.md` | 2026-07-22 | 206 | AUDIT — The Track + The Stable (the racing drops) |
| `AUDIT-ux-gameplay-flow.md` | 2026-07-19 | 178 | AUDIT — UI/UX & gameplay-flow (the client, the onboarding, the new-player journey) |
| `AUDIT-value-creation.md` | 2026-07-23 | 164 | AUDIT — The Value-Creation Drops (max-effort red-team, 2026-07-23) |
| `AUDIT-wire-step-four.md` | 2026-07-21 | 45 | AUDIT — The Wire step four (the Spymaster's Tradecraft + the Watchdog) |
| `AUDIT-wire-step-three.md` | 2026-07-21 | 86 | AUDIT — The Wire, step three: the counter-intel triad (DISINFORMATION + THE INFORMANT) |
| `AUDIT-world-frontier.md` | 2026-07-21 | 70 | AUDIT — World step four (THE FRONTIER MADE REAL) |
| `AUDIT-world-occupation.md` | 2026-07-21 | 70 | AUDIT — World step five: THE OCCUPATION (NPC-held core districts) |
| `AUDIT-tokenomics-v2.md` | 2026-07-27 | 112 | AUDIT — economy step 1 (THE EXCHANGE + THE FAMILY YIELD) |
| `AUDIT-tokenomics-v2-steps-2-3.md` | 2026-07-28 | 150 | RED-TEAM — economy steps 2+3 (the retirements, the rewritten buyback, the re-sourced float) |
| `AUDIT-full-sweep.md` | 2026-07-27 | 205 | AUDIT — the full line-by-line sweep (7 mechanical lenses over the whole tree) |
| `AUDIT-world-uprising.md` | 2026-07-22 | 79 | AUDIT — World step six (THE UPRISING) |
| `AUDIT-desk.md` | 2026-08-03 | 108 | AUDIT — THE DESK (economy steps 2–4: the recycle, the daily Dutch auction, the POL-fee buyback) |
| `AUDIT-oracle.md` | 2026-07-29 | 240 | AUDIT — the accretion oracle (OmrTwapOracle + OmertaBond wall 4 + the backend clamp) |
| `AUDIT-trades.md` | 2026-07-30 | 85 | AUDIT — THE TRADES pillar (steps 1-4), combined red-team |
| `AUDIT-street-life.md` | 2026-07-30 | 137 | AUDIT — STREET WAR step two + STREET LIFE (task #319) |
| `AUDIT-street-war-step-three.md` | 2026-07-31 | 186 | AUDIT — Street War step three (THE TAKE, revenge teeth, resident stables) |
| `AUDIT-favor-street-life-two.md` | 2026-07-31 | 195 | AUDIT — THE FAVOR + Street Life step two (escrowed player calls, corner chains) |
| `AUDIT-early-game-drops.md` | 2026-08-01 | 118 | AUDIT — the early-game batch (work board, trades strip, level-up, catalog, the crew door) |
| `AUDIT-strategy-package.md` | 2026-08-02 | 220 | AUDIT — the strategy package steps one to seven (sealed bid escrow, the watch, the roster, charters) |
| `AUDIT-economy-and-code.md` | 2026-08-03 | 200 | AUDIT — the full economic + code pass (the income ladders, the desk's anti-fabrication gate) |
| `AUDIT-npc-families.md` | 2026-08-03 | 119 | AUDIT — NPC families (the founder's cash writeback, the one-way flag, the starved recruit pass) |
| `AUDIT-exploit-hunt.md` | 2026-08-03 | 148 | AUDIT — the exploit hunt (six attack classes, no defect found; THE GATE MATRIX left behind) |
| `AUDIT-hired-hand.md` | 2026-08-03 | 78 | AUDIT — the hired hand (residents-in-crews): emission forfeit clean, double-hire TOCTOU (fixed 2026-08-04) |
| `AUDIT-blood-war.md` | 2026-08-04 | 94 | AUDIT — the Blood War + the Manhunt: clean; a second pass added the own_vassal gate + fixed a stale MANHUNT-deferred comment |
| `AUDIT-street-war-street-life.md` | 2026-08-04 | 120 | AUDIT — Street War + Street Life + the Bureau + residents (4 lenses): no CRITICAL/HIGH; fixed the hush-pay identity leak (D1) + the retiring-resident inbound-pointer class (F1/F2/F4, shared helper) |
| `AUDIT-hired-guns.md` | 2026-08-04 | 108 | AUDIT — THE HIRED GUNS + resident-crew interactions (4 lenses): no CRITICAL/HIGH, no §10.4 drift; fixed the retiring-hired-resident stranded-membership brick (F1, retirement-is-not-a-death parity, world raids + heists) |
| `AUDIT-family-war.md` | 2026-08-05 | 72 | AUDIT — THE FAMILY WAR formal declaration / npc_wars (5 lenses): no CRITICAL/HIGH, no §10.4 drift, persist-clobber safe, dissolution both-directions; fixed one LOW (conquering an outfit you're at war with now WINS the campaign — the conquest-blocks-war trap) |
| `AUDIT-resident-event-fills.md` | 2026-08-06 | 4 | AUDIT — the resident scheduled-field fills (tournament/GP/Stakes/Futurity, 4 lenses): no CRITICAL/HIGH, no §10.4 drift; Futurity empty-bet + retirement/death dead-burn + reactive-gate all verified clean, escrow-twin retirement-safety now tested not assumed; ONE flag (F1 — GP/Stakes are a bounded skill-dominated resident-seed EXTRACTION, not a fair redistribution — BALANCE-corrected, not retuned) |
| `AUDIT-blue-team.md` | 2026-08-09 | 177 | AUDIT — the BLUE-TEAM (defensive) pass (6 domain sweeps, 5 risk-ordered batches): no CRITICAL, the economic + auth core sound; hardened the deploy gate (autoDeployTrigger:checksPass), preflight (JWT-secret floor / webhook-collision / worker test-knob guard), the unauthenticated edge (security headers + fail-safe hook, trustProxy hop count, keyless-GET default-throttle, push-SSRF guard), detection depth (worker heartbeat → /health, exchange invariant nightly), and auth depth (mod_actions audit log + throttle, token_version revocation) |
| `AUDIT-nusd-market.md` | 2026-08-10 | 5 | AUDIT — THE BANK's nUSD market (NUSD/CollateralEscrow/Alchemist/Transmuter), a SELF-review of same-session code, nothing deployed: no oracle on the borrow path and no `liquidate()` by construction, RV finding #1 unreachable (escrows, not shares), 4 load-bearing guards mutation-verified; fixed F1 a redemption allowlist that contradicted the peg defense beside it, F2 an unseeded market that bricks after one borrow (now a stated deploy requirement + a test that proves it), F3 constructors trusting their own immutable wiring, F4 a reentrancy test that tested nothing, F5 a headline fuzz that was partially vacuous (only ever borrowed legal amounts); ACCEPTED: a sleeve loss breaks the supply invariant with no restoration — the buffer floor's stop-issuing-before-you-stop-paying is the answer, not a liquidation |
| `AUDIT-night-economy.md` | 2026-08-11 | 388 | AUDIT — the overnight ECONOMY lens (§10.4, faucets, the router, the severance): no drift, no unbacked mint; the participation faucets confirmed alt-reachable but NON-EXTRACTABLE by the severance, flagged to BALANCE rather than retuned; fixed four load-bearing comments still calling recycled sinks "burned / deflationary" and pinned the recycle hook's sign guard (a positive row with a sink reason must feed the desk nothing) |
| `AUDIT-night-exploit.md` | 2026-08-11 | 357 | AUDIT — the overnight EXPLOIT lens (hostile input, IDOR, races, rounding): every finding verified against source before any fix; no CRITICAL |
| `AUDIT-night-concurrency.md` | 2026-08-11 | 250 | AUDIT — the overnight CONCURRENCY lens (locks, persist-clobber, duplicate notifications): fixed the repeat-notification class with one `notifyOnce` helper across mentor offers, vouches and crew invites/requests |
| `AUDIT-night-contracts.md` | 2026-08-11 | 555 | AUDIT — the overnight CONTRACTS lens (5 findings + a PoC suite): fixed F1 a harvest that could push a healthy borrower under the invariant (the LTV/fee pair is now enforced in BOTH setters), F4 a "defence in depth, unreachable" clamp that IS reachable at dust scale (the fuzz's lower bound had excluded the only regime that reaches it — a lever measured in the wrong range), F5 a fee push inside the permissionless `harvest` that a blocklisted recipient would brick for the whole market (now accrues + a permissionless sweep, the OmertaHook rule) plus the two stranding addresses, F6 bond remainder dust to `address(0)` on an exact-10000 split; F3 RETRACTED by the lens itself (no over-charge, disproved end to end); ACCEPTED: `convertToAssets` is sleeve trust, not price risk, and the mint flow caps fail OPEN at zero — both now stated at the field and in CHAIN-DEPLOY §2b |
| `AUDIT-family-buyback.md` | 2026-08-12 | 5 | AUDIT — THE FAMILY BUYBACK Phase 1 + the token-health board (first-hand, the parallel workflow died on quota): no CRITICAL, no §10.4 drift; fixed F1 (MED) an unanchored first real buy — reproduced minting 1,000,000,000 $OMR from 1 ETH of revenue with the invariants green, because `credited == bought` compares two numbers derived from the same wrong price — now an ETH buy anchors on the canonical Vig print and an unanchored currency takes the treasury's deliberate-seed posture, and F2 (LOW) a stranded budget that read exactly like an empty one (the harvest ingest uppercases its symbol, the keeper did not); three further candidates recorded as died-on-checking; then F3 (MED) by sweeping the CLASS — `recordBankBuy` walled the spend but not the RATIO (1 USDC of real profit booked 1,000,000,000 $OMR, invariants green), and that pool funds the withdrawal reserve |

| `AUDIT-bank-city-leg.md` | 2026-08-12 | 5 | AUDIT — the Bank's city leg (`src/bank.js`), the day-old module that MINTS `prize:omr` and funds the withdrawal reserve: **no findings at any severity** — recorded so the sweep is not repeated. Attacked and clean: the absolute pool write is safe because BOTH writers go through a `FOR UPDATE` singleton read (the `foundNpcFamily` lost-update shape); pool-then-accounts inverts the house lock order but cannot cycle, because no path holds an account and then reaches for the pool; `distributed <= bought` is not a blind check, because both counters are validated against their own rows and the ratio feeding `bought` is bounded by the F3 price wall; the distribution is Sybil-NEUTRAL algebraically and the two gates are Sybil-NEGATIVE; a rolled-back action rolls back its score (activity is written on the caller's client, as COUNTS not XP); the post-commit `fundReserve` gap really is named in the Vig sandwich; and the payer is actually wired. Two non-defects recorded so they are not re-derived: the remainder lands on the last sorted payee in an unreachable branch, and the distribution weight is deliberately coupled to `MASTERY.XP` |
| `AUDIT-session-scarcity-poll-contracts.md` | 2026-08-16 | 9 | AUDIT — today's three drops (THE FIRSTS / LIMITED RUNS / THE SHIPMENT, the population-scaled cap, the poll decoupling) **plus the 18-contract suite read as a GRAPH** (shared keys, token flows, walls that span contracts), first-hand: no CRITICAL. F2 the headline — a fire-kill DESTROYED the shipment instead of looting it, because the block copied its NUMERIC direct-SQL neighbours while `shipment` is INT **and** persisted, so the victim lost it and the killer's SQL credit was clobbered by the persist (untested; the suite header claimed it as proven); F1 a swallowed 23505 that turned a retryable contention into a raw 25P02 500 (reproduced) — the swallow's promise cannot be kept inside a transaction; F6 the poll decoupling left cooldown BUTTONS stale up to 2 minutes while the clock read READY (the control-that-lies class arriving through TIME) → now a zero-crossing re-render, guarded as mobile check H; F4 a stale-read announcement, F5 an N+1 on a polled board, F3 a cached SAVEPOINT probe. Contracts: C4 (MED) a re-imported Street Deed kept its on-chain identity and would deliver the PREVIOUS owner's stock into the next extractor's ERC-6551 vault with every invariant green; C2 (MED) an unconfigured stock ticker delivered UNCAPPED (the cap is a mapping and the ticker set grows) → `defaultDailyCap`; C1 (MED, ops) one key signs four contracts with NO rotation runbook → the ordered runbook + a docs guard that every signer-bearing contract joins it; C5 two contracts both claimed to be the stock container (the NatSpec IS the spec for an audit); C3 the honeypot ceiling is per-layer and the seller pays the SUM → one venue, one layer. **Two guards survived their first mutation and are written up for why** |
| `AUDIT-full-red-team.md` | 2026-08-16 | 10 | AUDIT — full red team over the game AND the 18-contract suite, first-hand (every finding reproduced against a running engine or the contract source before it was called one): **no CRITICAL, two HIGH, four MED, four LOW**, all fixed with mutation-verified regressions. H1 a StreetDeed voucher was reclaimable by the VoucherClaim rail (`vouchers` is one table, two contracts, independent nonce spaces) — not a lost voucher but a street bricked FOREVER in extraction-pending, on the ordinary abandoned-extraction path; fixed with an allowlist, never a denylist, so the third contract to share that table is invisible to the old rail by default. H2 only the FIRST delivery of each stock allocation ever landed — `allocateStock` accumulates into the PK while `deliveryIdFor` was keyed on the PK alone, so a second tranche computed an id StockVault had already consumed (the default operating shape, not an edge); fixed with a running total in the id derivation + a backfill, because without one every historical delivery re-plans in full after deploy — a double delivery of real stock. M1 `VoucherClaim` still enforced a LIFETIME gear cap while `GearVault` had moved to LIVE supply, so a re-imported item could never be re-extracted once a class had ever hit its cap — fail-closed either way, which is why it was invisible; what it killed was a shipped feature. M2 the allocation builder lowercased every wallet, and base58 is case-sensitive — an entire Solana community's envelopes orphaned forever, silently, with the counts and the published commitment reading correct; the LOADER had the rule right and the builder sat upstream of it, so one shared normalizer replaced two implementations of one rule. M3 `quoteBond` held a row lock across an oracle RPC (the `bankPosition` pool-exhaustion shape, on a write path). M4 the documented 200,000-row drop batch was unreachable behind fastify's 1MB default — it surfaces on launch night, once, under time pressure. **Two candidates were investigated and REJECTED, and are written up at length**: the track's player-entry economics (a quoted +8.0% that was the mean of the already-flipped subset; my own unconditional measure was −7.19%, and my first correction was itself wrong twice — 0 of 20,000 profitable once the runners are actually fieldable), and a stale delivery target where I WROTE THE FIX BEFORE PROVING THE BUG and then found two walls already there, so the unreachable third wall was removed rather than kept for comfort |
| `AUDIT-red-team-two.md` | 2026-08-16 | 7 | AUDIT — the SECOND max-effort red team, run straight after the first merged, first-hand throughout (nothing fixed before it was reproduced; three findings needed real Postgres because pg-mem cannot exercise them at all): **no CRITICAL, four HIGH, two MED, one LOW-MED**, every fix mutation-verified by name. C1 (contracts) `OmertaHook.setSurge` could arm a fee while every recipient was still address(0) — its two siblings refuse on that exact state, and the deploy order arms the surge first — after which the PERMISSIONLESS sweep paid the accrual to address(0): reproduced on a real PoolManager, **4.75 ETH burned irrecoverably**. It is also the worse kind of process finding: reproduced in the PREVIOUS session, left in an untracked scratch file, and caught only by reading `git status` before committing — exactly what that pass's own process note had warned about. H1 a street could be SOLD in the city and taken ON-CHAIN by the same man — `listDeed` refuses an on-chain deed and `requestDeedWithdraw` refuses a listed one, and neither could see the other because list read the row UNLOCKED while extract read it FOR UPDATE; reproduced end to end, the buyer pays $500k, the seller claims the NFT, and `markDeedExtracted` re-keys the row leaving the buyer with no street, no legend and no cash. Fixed at BOTH the cause (the lock) and the point of disposal (the wall), because a wall at the cause alone trusts that no future writer of `sale_price` ever gets it wrong. H2 `POST /v1/auth/x/start` called `req.jwtVerify()` directly instead of the `auth` preHandler — a verified signature is not a usable token, and the ban + `token_version` checks live in the preHandler, so a session already killed by `logout-all` could still permanently bind an attacker's X identity to the victim's account. H3 two of 109 `pool.connect()` sites leaked their client (`sweepNpcWars`/`sweepFamilyAggro`, beside three siblings in the same file that do it right) — a leaked pooled connection is permanent and cumulative, so the worker goes dark and takes the nightly §10.4 sweep, the drift alarm, the backup watchdog and every timed settlement with it, indistinguishably from a quiet night; measured on real Postgres, and closed as a CLASS with a new `test/gates.js` connection ledger. H4 the yard boss's protection followed you onto the street — $15,000 bought 2h untouchable by fire/jump/npcHit/shank with NO actor restriction, against a safehouse's $25,000 floor that also stops you acting; its sibling `inHole` had already been capped at the sentence for this exact reason. H5 a free level-1 alt was a $48,000/day corner faucet (the sim's own model assumes one deed per PLAYER; it is one per ACCOUNT) — the floor went on the MONEY, not the claim, so a newcomer still names their street. H6 a shank thrown from a hospital bed: the game already refuses a shank ON a man in the infirmary and had no gate on the attacker being in one. H7 THE FIRSTS admitted residents and agents to a one-per-server-EVER trophy that is consumed rather than re-taken. **Also recorded: my own §10.4 classifier reported 13 false orphans** — two bugs in the tool, not the tree; a finding produced by a tool you wrote and did not check is not a finding |
| `AUDIT-red-team-three.md` | 2026-08-17 | 178 | AUDIT — the THIRD max-effort red team, aimed deliberately at what the first two leave open by construction: modules that appear in ZERO audit report (`grep -l` across all eighty-odd reports returns nothing for `citywire`, `dexbot` or `stockdeliver` — two of which move real value), and classes the earlier passes established but did not sweep to their edges. First-hand throughout: **no CRITICAL, one HIGH, two MED**, every fix mutation-verified by name, and three lens sweeps recorded CLEAN because a red team that publishes only its hits cannot be audited. F1 a REVOKED token still opened a live websocket — there are four ways a bearer is accepted and `token_version` was checked in three; `logout-all` (the self-serve answer to "someone has my session") killed REST while the thief's already-open `me` feed streamed on indefinitely, and `mod/revoke` DID cut live sockets but with no connect-time check the same dead token reconnected instantly, so each half defeated the other's remedy; fixed at both. F2 the street deed name accepted homoglyphs, zero-width and bidi while every other name in the game carries the R8 charset guard and this function's own comment claimed it did — it matters MORE here, because `tokenId = keccak256(bytes(name))` makes the name the permanent on-chain identity, `name_lc` uniqueness is defeated by a Cyrillic look-alike (reproduced: a second visually identical street claimed cleanly), and the asset trades on a secondary market with real stock deliverable into its vault. F3 a staged delivery outlived its deed: `stageStockDelivery` re-resolves the target correctly and then DISCARDED it on the duplicate path, so the keeper's claim read a stale `tba` — reproduced end to end, real stock delivered into the vault of a deed its recipient had already SOLD, with every wall green because they are denominated in UNITS and who received them is not a quantity. **F3 is the class the previous pass examined and dissolved**, and that write-up is still right about what it checked: it checked the resend-within-window path (two walls hold) and not the re-target-after-a-sale path, which re-enters the same pending row with a valid NEW target — both walls pass and the row is stale |
| `AUDIT-red-team-four.md` | 2026-08-17 | 206 | AUDIT — the FOURTH max-effort red team, aimed at the last module that appears in ZERO audit report and happens to be the one that sends real ETH (`dexbot.js`), and at a class the earlier passes established without sweeping to its edge (the watchers' per-log isolation rule). First-hand; two of the three findings needed real Postgres because pg-mem is single-caller and cannot drive them at all: **no CRITICAL, one HIGH, two MED**, four mutations each failing at its own named assertion, plus three lens sweeps recorded CLEAN. F1 both DEX keepers read an UNLOCKED budget and then SEND, and the send is the irreversible half — reproduced on real Postgres, two overlapping workers put **4 ETH out on a 2 ETH budget** and **2 pairings on 1 ETH delivered**, so real ETH left the wallet with no accounting at all; the invariants caught both the same night, which is the DETECTOR, and the money had already gone. The worker's comment asserted the opposite ("bounded by the module's own root caps") — the caps bound the BOOKING, never the send, and a wrong claim in a comment licenses the next reader; fixed with the `runStockBuyback` advisory-lock pattern, one class per bot. F2 one poison fill wedged BOTH bots forever: `bookUnbookedSwaps` runs FIRST (crash recovery), `runVigBuyback` legitimately refuses a fill more than 10× off the last print (the fat-finger wall doing its job on a real fill), and that refusal escaped — so one unbookable fill blocked every FUTURE swap, and because neither keeper was `safe()`-wrapped in the worker it took POL pairing down with it; fixed with per-row isolation (the row stays UNBOOKED on purpose — booking past the wall defeats the wall) plus `safe()` and loud logging. F3 a malformed harvest log wedged its cursor forever while the function's own comment promised the protection — reproduced with a dust harvest whose fee rounds to zero (cursor stuck at 0 across every tick, the good fee behind it never booking, and the Bank's revenue IS the city leg's budget); the POISON set had been grown once for the stream that prompted it, so it now lists the CLASS, guarded by a new `test/gates.js` WATCHER POISON LEDGER (catalogue-or-declare) **which found two more on its first run**. Clean lenses: `nft.js` (rarity is display-only in-game, so the missing escrow gate cannot mislead a bidder — the one path where it becomes real does carry it), 180 hostile-input calls on the newest money routes (**and the vacuity correction is the part worth keeping** — the first run's clean result was 8 of 10 probes bouncing at a precondition, one testing nothing at all), and cursor discipline across all twelve watchers |
| `AUDIT-red-team-five.md` | 2026-08-17 | 173 | AUDIT — the FIFTH max-effort red team, one game half and one contract half, aimed (like RT#4) at what the prior reports leave open by construction: modules appearing in ZERO report, and classes the earlier passes established without sweeping to their edge. First-hand; two of three findings needed real infrastructure (real Postgres for the meter's concurrency, a real Foundry VM for the contract default): **no CRITICAL, three MED**, eight mutations each failing at its own named assertion, plus three lenses recorded CLEAN. F1 THE DISPATCH mailed addresses nobody had proved they owned — a typed string was treated as proved, so three free guest accounts sent three unsolicited digests to a third party who had never touched the game, with the one-click unsubscribe confirming to them that the address was live; fixed with real double opt-in (an `email_verified` gate on the SWEEP, an HMAC bound over account AND address so retyping drops the proof, a per-ADDRESS claim-then-send confirmation meter across accounts so the confirmation mail is not itself the spam vector, and one proved owner per address enforced on BOTH the set and confirm paths). F2 `/health` was keyless, DB-heavy and unthrottled — the BLUE-TEAM H4 denylist-by-default is scoped to `/v1` and this route is not, so a 400-request burst that `/v1/city` cut at 30 served all 400 at two DB queries each; fixed with a 2s TTL + single-flight rather than a 429, because **a 429 misleads a monitor in both directions** (200 concurrent → 2 queries, a serial flood → 0). **The vacuity lesson twice in one finding**: the single-flight mutation survived two runs — a direct probe showed it costs 400 queries against 2, so the fix was load-bearing and the TEST was wrong (earlier assertions leave the cache WARM, so the burst never reached the branch). F3 two of the four contracts sharing one `VOUCHER_SIGNER_PK` deployed with NO rate wall and the runbook called one of them "Optional" — RT#2 established that the key's blast radius is the SUM of four daily caps; two are constructor args and cannot be forgotten, two were setter-only at `0` = unlimited, and a fresh deploy on a real VM minted **500 deeds and 500 identities in one day** with nobody doing anything wrong (`DynastyNFT` has no supply cap at all, and a `StreetDeed`'s ERC-6551 vault is where real tokenized stock lands); fixed by making the cap a CONSTRUCTOR argument on both — 0 is still legal, so it forces a DECISION at deploy rather than a number — guarded by extending the existing signer-rotation block in `test/docs.js`, and free to do now only because the audit batch has not run. Clean lenses: `brokers.js` activation (the nonexistent-row `FOR UPDATE` gap is unreachable behind the char lock), every non-`/v1` route (the five DB-touching ones are throttled by prefix — which is what confirms F2's scoping was complete), and the `OMR` sell-tax zero-address guards + the `0 = unlimited` cap convention |
| `AUDIT-red-team-six.md` | 2026-08-17 | 210 | AUDIT — the SIXTH max-effort red team, aimed where the founder pointed it: edges the game has not been combed through, and INTERACTIONS (within the game, and between the smart contracts). First-hand; F1 needed real Postgres (a two-writer race pg-mem cannot drive) and the wealth sweep needed a booted server with two real characters: **no CRITICAL, no HIGH, two findings** and six lenses recorded CLEAN, four mutations each failing at its own named assertion. F1 (MED, LATENT) the 12h buyback timer was never re-checked under the `street_tax` lock — the peek is authoritative for the POOL and only the peek for `last_buyback`, so two overlapping workers both clear it and the loser carves a SECOND time in one period (reproduced with a third connection holding the lock: **51,000 funded against a 30,000 period at FUND_BPS 3000**); inert at the shipped 10000 because the winner drains the pool, i.e. a cadence gate holding by an accident of an unrelated founder lever — the third defect in this codebase that a later lever change would arm. Fixed by re-reading BOTH halves under the lock; the regression also gave the cadence its FIRST coverage (every prior buyback assertion passes `force`, which bypasses the timer). F2 (LOW) the sixth vehicle handover kept the nitrous — five of six `character_id` re-points clear consent flags AND the consumable, `stealCar` cleared only the consent half and read deliberate because its own comment names just those; made consistent and guarded by a new **THE HANDOVER LEDGER** family in `test/gates.js` (nine handovers, one waiver, anti-vacuity floor). Clean lenses: **the exact-wealth sweep** — 172 authed GET boards called as a STRANGER against a mark holding five collision-proof figures, with two vacuity controls (the detector finds all five on the mark's own sheet; 12 boards genuinely CONTAINED the mark, including the paid wiretap, dossier and Street Wire) → zero leaks; the Bank cluster read as a GRAPH (the two singular fail-closed authorities, the burn-your-own-balance caveat honoured in code, the §2.4 issue-before-pay ordering, and a chased-and-DISSOLVED performance-fee-on-principal suspicion — `totalAssets − principalOf` is invariant under a withdrawal); every ownership transfer excluding an extracted item; absolute writes to `account_persistent` from an unlocked read; the shared `vouchers` table (including a FALSE POSITIVE from my own grep, recorded); and all 52 $OMR reason literals classified with no `kitchen:module`-class orphan. Flagged: `ALCHEMIST_ASSET_DECIMALS` is trusted from env while the contracts read it off the token |
| `AUDIT-red-team-eight.md` | 2026-08-17 | 3 | AUDIT — the EIGHTH max-effort red team, three more of the shape RT#7 named (a class established, applied where discovered, never swept to its edge) plus the one question nobody had asked directly: **can the §10.4 alarm actually fail?** First-hand; two lenses needed a booted server driving real routes and the contract lens a real Foundry VM. **No CRITICAL, no HIGH; one MED, two LOW-MED, three lenses CLEAN**, seven mutations each failing at its own named assertion. **F1 (MED) an object index is not an allowlist** — `if (!CATALOG[userInput]) throw` reads like a membership test and is not one, because `__proto__`/`constructor`/`toString`/`valueOf`/`hasOwnProperty` all index TRUTHY. Eleven such gates; five take user input; **two were live defects, both reproduced through the real route**: `KITCHEN.MODULES[modId]` reached the `lab_${modId}` COLUMN NAME and 500'd, and `MASTERY.TRAITS[traitId]` returned **200 and WROTE `trait_id='__proto__'`** — silently and permanently consuming the once-ever level-50 capstone with a trait that then grants nothing, after which the `chosen` gate blocks the real choice forever. **Injection was never possible** (a quoted payload is not a prototype key, so the gate catches it) — which is exactly what made the gate look adequate. All five fixed with `Object.hasOwn`, and guarded by a new **THE CATALOG LEDGER** whose extractor had to learn BOTH forms, since a scanner that only knows the broken shape stops counting a site the moment it is fixed. **F2 (LOW-MED) one poison stream starved the other ten, and bypassed RT#4's own fix** — eleven chain-sync watchers, the stock delivery keeper and the two DEX bots sat inside ONE try/catch, so an outer catch defeated the inner `safe()` wraps RT#4 added for exactly this failure; a throw in the first sync skipped every job below it, every tick, quietly. Fixed with 13 individual wraps **plus the half that is easy to get wrong** (`safe()` returns null, and eleven call sites immediately dereference it — the first cut turned a contained failure into an uncontained TypeError in the next statement), guarded by **THE ISOLATION LEDGER** (76 jobs). **F3 (LOW-MED) six contracts hand ownership over in one step** — ten inherit `Ownable2Step`, six inherited plain `Ownable` with no stated reason, the forgotten-sibling shape; `OMR` matters most since its owner holds `setMinter`, the only mint and its own emergency stop. The deliberate 'renounce = freeze forever' hatch **survives** (Ownable2Step overrides `transferOwnership`, never `renounceOwnership`), proven by two new behavioural Foundry tests; guarded in `test/docs.js`. Free now, a re-audit later. Clean lenses: **invariant vacuity — 31 of 31 §10.4 checks fire on a planted violation and 31 of 31 restore**, with 31 declared == 31 emitted so none is silently skipped; client stored XSS (14/16 fields reached the guard, 0 stored raw markup — the probe's FIRST run was vacuous, 11 of 13 bouncing at a precondition, and was rebuilt); and all **95 interpolations inside a SQL template** traced to their assignment with no user string reaching SQL text. **M6 first appeared to survive on ORDER, not on the check** — renaming a waiver trips the bare-index assertion first, so a stale-waiver check can only be verified by ADDING a bogus one |
| `AUDIT-red-team-nine.md` | 2026-08-17 | 3 | AUDIT — the NINTH max-effort red team, aimed at the SEAM rather than either side of it: what the backend encodes, decodes, signs, restates and configures against what the contracts actually declare. RT#1–#8 read the contracts; none crossed the two sides field by field. **No CRITICAL, no HIGH; three MED, four lenses CLEAN**, four mutations each failing at its own named assertion. **F1 (MED) the chain performs a split the backend restates, and nothing ever compared them** — `OmertaFees.vigBps` and `OmertaBond.polBps/devBps/rwaBps` are IMMUTABLE, hand-set at a deploy the script does not cover (8 of 16 contracts are hand-deployed; `OmertaBond` takes twelve args, three adjacent same-typed bps and four adjacent same-typed recipients). The fee half is the sharp one: `MintFeePaid` carries the GROSS ALONE, so `recordVigRevenue` DERIVES the share from `VIG_BPS` — a divergence directly mis-books `vig_revenue`, the withdrawal reserve's funding source, and every check sums either way because they all compare figures derived from the same restated number. The realistic trigger is the env flip (`deploy/fee-splits.env` on BOTH api and worker) missed on one service. Fixed with `onchainSplits()` + `splitParity()` alarming hourly through `alertDrift`, plus the control-room read that was `read: null`. **F2 (MED) the identity NFT had no entrance** — `DynastyNFT` mints ONLY against a signed `MintVoucher` and has no owner mint, and NOTHING in the tree signed one (zero hits for `OmertaDynasty`/`MintVoucher`); three of the four contracts sharing one signer key had a signing route and this one did not, while both the runbook and the log said the rail was complete. It fails CLOSED — the product simply does not work — but the exit half (watchers, registry, portrait freeze, metadata, royalties) was complete and looked complete, so an audit would be paid for and a contract deployed that nobody can use. Fixed with `requestDynastyMint` + `POST /v1/identity/mint` on the founder's recorded gate, one per account on both horizons; RT#8's `VOUCHER_CLAIM_KINDS` allowlist earned its keep one drop later (the new kind is invisible to the reclaim rail BY CONSTRUCTION). Guarded: every signer-bearing contract must have a backend route signing in its EIP-712 domain. **F3 (MED) the stock keeper guessed a token's decimals** — the class AUDIT-red-team-six established for the Alchemist and did not sweep, surviving as ONE `STOCK_TOKEN_DECIMALS || 18` for a ticker→token MAP, used to size the real `StockVault.deliver`. Asymmetric: over-sending REVERTS at the ERC-20 (loud), **under-sending SUCCEEDS** and the ledger books the staged units, so a player is told they received N shares and receives a millionth of one, with `allocated <= held` and `delivered <= allocated` both green because both compare ledger numbers. Fixed by reading `decimals()` off the token, bounded, cached, throwing rather than falling back; the knob DELETED. Clean lenses: all 14 backend event signatures crossed on type, indexed-ness AND positional name (the sharp risk is a same-typed adjacent swap — `Bonded` has six adjacent uint256 — which a type-only check cannot see), the two external ones verified against `v4-core`'s `IPoolManager` and OZ's `IERC721`; all 8 deploy-script constructors correct on arity and order; the four EIP-712 domains distinct and three matching field-for-field. **Two candidates DISSOLVED**: the runbook's bond split 'contradicting itself' (the levers are env-backed and `fee-splits.env` is the signed flip — what survives is its shadow, which is F1), and my own sweep's `✓ read` for the bond bps (a false CLEAN — the regex matched an object KEY on the board, not a contract read). |
| `AUDIT-red-team-seven.md` | 2026-08-17 | 1 | AUDIT — the SEVENTH max-effort red team, aimed at the shape RT#4–#6 leave open each time: a class this project has ALREADY established, applied where it was discovered, and never swept to its edge. First-hand; the headline needed a booted server driving real routes: **no CRITICAL, no HIGH, one MED across 27 boards** and five lenses recorded CLEAN, three mutations each failing at its own named assertion. F1 human status boards rank NPC SCENERY — the population layer's own recorded rule ("a future step that gives residents a legend must exclude them on that board at the same time") was applied per-board as each was discovered and never as a class, while residents kept accruing legends (fighters, racers, stables, duels, deaths, heirs). REPRODUCED end to end: the duelling ladder is the sharpest case because it has NO threshold at all — it ranks raw `duel_elo`, which DEFAULTS to `ELO_START`, so it needs no play whatsoever to fill with bots (**6 of 7 entries residents, the server's only human sitting sixth on an identical rating**); and the ancestral hall follows from the population layer's most deliberate decision — *death is deliberately not special*, so a killed resident runs the ordinary `runEstate` and writes a `bloodline` row, putting a resident house on the great-houses board at **score 520** while the only human was not on it at all, because he had not died yet. Not a leak (no §10.4 surface, nothing farmable) — it damages what a status board is FOR, and it is worst on a thin launch-night server, the only condition under which residents outnumber players. Fixed at 27 boards AND guarded: a new **THE SCENERY LEDGER** in `test/gates.js` (catalogue-or-declare, 11 waivers each with a board-property reason, an anti-vacuity floor, a stale-waiver check), checking **per RANKING STATEMENT rather than per board body** — which is the whole reason it works, since `duelLeaderboard` runs two queries and a body-level check reads a half-guarded board as protected. **M1 survived its first two runs and both causes are recorded**: the body-level blind spot, then my own SQL comments containing BACKTICKS inside JS template literals, which terminate them (three files broken at parse time; `pgquery` reads SQL as text and cannot see it, only `node --check` catches it) — a trap already in CLAUDE.md, hit a second time. Clean lenses: the pause matrix across all five pausable contracts (no contract can be paused into a state where value is unreachable by anybody); a persist-clobber RE-sweep after ~15 new columns (66 persisted char / 18 account cols → 4 actor-row hits, all legitimate) — **whose extractor first reported "0 columns" and nearly went in the report as a clean sweep**; keyless-route throttle scoping after this session's new routes; the three zero-coverage money modules (`made`/`career`/`hustle`); and `commissionPiece`'s COUNT+1 serial (PK backstop → clean `contention`, by design) |
| `AUDIT-red-team-ten.md` | 2026-08-17 | 2 | AUDIT — the TENTH max-effort red team, aimed at the two remaining instances of the shape RT#7 named (a class established, applied where it was discovered, and never swept to its edge): **lock order**, fixed four separate times at four separate sites and never checked as a whole, and **error mapping**, fixed at three. First-hand; two lenses needed real Postgres (a live database older than the build; a lock property pg-mem cannot express). **No CRITICAL, no HIGH; two MED, seven lenses CLEAN, two permanent guards**, seven mutations each failing at its own named assertion. **F1 (MED) a lock pair with two orders, and a safety argument that was FALSE** — `callOutChamp` locked `boxing_title`→`fighters` and argued the inversion safe on a stated precondition (*'any counter-path that would lock this fighter must first block on the held caller char'*) that `acceptCallout` — a function the same comment NAMES as canonical — violates in its own explicit words. Reachable only through a narrow window (the callout must clear between acceptCallout's unlocked read and its locked one, which the belt-defence sweep does) and bounded to a 40P01 → `contention` retry, so no money was at risk: **the defect is the argument**, because a comment asserting a precondition its own named sibling violates is what licenses the next edit. Fixed by taking the order rather than reasoning about it (fighter → title, TOCTOU re-verify), and guarded by a new **THE LOCK LEDGER** — two rules (no pair locked both ways; no singleton before a `characters` row, which rule one is blind to because `withCharacter` already holds it) over 74 multi-lock transactions and 46 pairs, with two anti-vacuity floors that fail differently. **Splitting on transaction boundaries is what makes it usable, and that is measured**: without the split the sweep reported two false positives out of three. **F2 (MED) transient contention reported itself as a server bug** — the wrappers map 40P01/23505/55P03, but 89 functions open their own transaction and eight are reachable straight from a player route (the whole withdraw/gear/item/deed/dynasty rail, the bond quote and claim), so those answered `500 internal`. A `55P03` there needs no deadlock at all — just 8s of ordinary contention on a singleton like `chain_reserve` — and 500 tells a client to back off where the honest instruction is 'try again now', which costs agents most since they read these codes. **The fourth instance of a class the same handler already argues twice** (db_down, and Fastify's own 4xx). Fixed ONCE at the handler rather than at 89 call sites, reusing `deadlockToRetry`'s own code set so the two cannot drift; the regression drives the real handler through test-only routes and asserts BOTH halves, so a blanket swallow fails too. Clean lenses: **the migration class** — `pgcheck` §7b now boots the current build against the OLDEST schema in the history and proves the 2026-08-06 outage defeated, establishing two things that were not known (the deriver contributes NOTHING today, because the hand-written ALTER discipline covers the lot — so it is the belt to those braces, and a green means 'nothing forgotten yet'; and the assertion's shape was reached by discarding two wrong ones, the second of which reported **17 phantom missing columns**); day/week boundaries; unit boundaries swept three ways (the class with a 2/2 hit rate); the idempotency perimeter (no PUT or PATCH routes exist at all, and ban + revocation are checked on both paths); and **two-party transfers with independent legs** — worth a lens because §10.4 is structurally BLIND to it (debit 100 / credit 101 moves Σcash and Σledger equally), 402 of 404 ledger calls parsed, every same-reason pair crediting the pool or burning. **Three of my own tools gave wrong answers and all three were caught before anything was reported** — the lock sweep's 2-in-3 false positives, a transfer sweep that matched **0 debits out of 404** and reported '0 findings' as though it meant something, and a take-accounting sweep blind to delegation through `payVig` |

Generated by hand; `test/docs.js` fails if a report exists that is not listed here, or if this
file lists one that does not exist.
