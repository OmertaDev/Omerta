# Bounded Family CASH/AMMO custody

`tools/rc1-family-cash-ammo-journal.js` reconciles complete committed snapshots with exact decimal strings and authoritative canonical outcomes. It classifies formation's personal cash sink, personal cash tribute to one identified Family, car melt's personal ammo plus Family cash/ammo creation, and last-member dissolution's cash/ammo destruction. Membership, rank and leadership changes alone create no resource flow. Per-character cash+bank and ammo ledger parity retain the other endpoints; unrelated personal receipt semantics remain outside this journal.

The source-authored paths have no generic personal treasury withdrawal or personal ammo-bank deposit/withdrawal endpoint. The Family ammo bank receives melt tithe and is destroyed at dissolution. Other Family spending/income, war, turf, contracts and territory remain unsupported; OMR/reserve reconciliation belongs to a separate journal. An unsupported receipt produces `PARTIAL_UNSUPPORTED`, and unexplained balance movement fails. There is no residual adjustment or floating-point tolerance.

The adapter accepts `snapshotFamilyCashAmmo(pool)` or complete retained native `tables` snapshots. `reconcileFamilyCashAmmo(before, after, {operations, familyIds})` requires the actual POST account/character, request identity and canonical response; it never accepts a replay as new movement. A character crossing multiple Families requires finer committed boundaries. It preserves all old transaction rows and checks recipient, owner, amount, sign, car disposal and Family endpoints. Every native boundary retains the complete before/after custody input. The shared global observer remains unchanged and its unknown classifications remain visible.

Run `node test/rc1-family-cash-ammo-journal.js` for lifecycle and corruption controls. Run `node test/rc1-family-cash-ammo-native.js --postgres` with `COORDINATION_TEST_DATABASE_URL` pointing to the disposable local control database and `RC1_FAMILY_CUSTODY_OUTPUT` naming a fresh restricted directory. A clean immutable commit is mandatory. The proof creates two ordinary accounts/characters, declares only founder level75 respect eligibility before baseline, and uses the actual daily check-in to fund formation. No direct balance, item, status or membership grants occur. Measured formation, nonzero tribute, natural GTA acquisition, melt, succession and dissolution use canonical APIs. One scoped late ammo-ledger fault must roll back completely before the exact request is retried. Original worker callbacks accompany any public cooldown waits.

`test/rc1-family-cash-ammo-retained.js` accepts `RC1_RETAINED_ROOT` and fresh `RC1_FAMILY_REUSE_OUTPUT`. It verifies sealed artifact hashes before independently reclassifying the original-lifetime Family D window from `7df52b3f` and the two social windows from `5227fe19`. Historical intermediate equations do not substitute for missing complete snapshots: this audit covers the retained initial/final windows only. Legacy request payloads were overwritten by response bodies and are explicitly unavailable. Invocation/completion order is not database commit order. A historical audit is not a fresh-source native pass, and no existing evidence is rewritten.

This component does not clear the full resource taxonomy, matrix, 90-day workloads or production release gates. Native and historical execution results are documented separately after sealing.

The first native attempt at `0c0f134b` is retained as `FAIL`: the final diagnostic assertion rejected the exact deliberately injected HTTP500 as a worker-recorded console failure. Rollback, full snapshot equality, trigger/function cleanup, same-key retry and replay had completed. The corrected harness requires precisely that one declared fault and no others; it preserves the original diagnostic rather than suppressing it. This was a harness expectation correction, not a gameplay repair.

## Sealed execution results

The fresh native proof at clean immutable source `9cef58c3a3bf774366b17064ad0461e4318d6d26` passed its bounded scope on 2026-09-21, 08:09:57.568–08:10:22.524 UTC. Restricted output is `family-custody-9cef58c3-native`. Two ordinary entrants used the one declared initial respect fixture. The founder canonically formed the Family; its member paid $200. The first natural GTA attempt produced a car, and melting it created 18 personal rounds, 5 Family rounds and $150 in the treasury. Promotion and boss succession preserved $350 and 5 rounds. Last-member dissolution destroyed those exact Family amounts. The deliberate late ammo-ledger failure left the entire normalized canonical snapshot unchanged; cleanup removed both trigger and function, the same key then succeeded once, and the completed tribute/dissolution each replayed with full snapshot equality.

The journal retained 12 complete custody boundaries, 22 per-Family equations and 48 personal endpoint checks. All55 canonical invariants passed at19 boundaries. The unchanged shared observer retained132 equations over18 boundaries plus11 unknown classifications (check-in1, Family lineage4, tribute1, table-change2, melt tithe1 and dissolution2). Those labels were not silently cleared by this separate journal. Original workers booted, but first-attempt GTA success required no cooldown advancement: elapsed logical time and due recurring callbacks were both zero. This run does not claim any timed Family branch. Fault objects and the exact owned database were cleaned up;50 indexed artifacts and74 hash-chained history events verified.

Native configuration SHA-256: `32e4b1771bfc4fa1825602c7c1914edf23370bea33e23f80f05da342afdcbfd4`. Sealed `run.json`: `1db10c7fd47fb34bd5af197dd6226b4b8068a2ab4a7114457db7bbd6e62dcd31`. Final canonical state: `6c19653bd05d597488479868674b53c121679a29638c463fcb035eee0b7bda3c`.

The independent historical audit at the same observer source passed three retained aggregate windows in `family-custody-9cef58c3-retained`. Original-lifetime source `7df52b3f35c516215427a8b88040628446837175` Family D reconciled $1000 tribute, $180/6-round tithe creation, 18 personal rounds and $1180/6-round disposal. Its original A/B/C war/turf branches and missing intermediate complete snapshots remain outside this audit. The monopoly and fragmented windows at `5227fe1902315e177ea9fe3b7ceb9ae9f1ae1712` reconciled measured Family tribute of $100 and $200 respectively while membership-only transitions moved no cash/ammo. Each case rejected an actual retained receipt with a corrupted Family recipient. Historical audit SHA-256: `4595627397465497b571f95c7203b13949114b670aaa9be0a6642b3acec044b9`. The earlier `0c0f134b` historical audit is also preserved. Neither historical result inherits a fresh-source native pass or upgrades the old fixture/invariant scope.

The unit suite passed20 corruption controls, including wrong recipients/owners, missing or duplicated receipts, altered responses, retained melted cars, cross-Family allocation, altered historical receipts, tiny decimal drift, orphan membership and a Family created/deleted outside observable endpoints. No production runtime files changed.
