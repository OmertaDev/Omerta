# Crypto field guide

Continue the existing Beautiful UI direction: ink/paper/brass/teal, the shared Omerta display font, ruled editorial sections and semantic HTML. Six linked exhibits separate control, cash flows, execution constraints, season state, account identity and release evidence.

Interactions use explicit previous/next buttons, native selects, labeled numeric inputs and checkboxes. Receipt validation clears stale amounts and disables advancement. Reserve scenarios expose all four illustrative bounds. The identity gate explains both missing access and insufficient account independence. The season ends with disabled advancement; previous remains available. No wallet, production request, authentication or paid generation occurs.

At 760px columns stack; tables retain horizontal scrolling in labeled keyboard-focusable regions. Shared focus outlines and reduced-motion behavior apply. Static surrounding copy and evidence remain readable without JavaScript. Generated PNG/SVG ownership artwork is illustrative and carries implementation labels.

Verification scope: teaching-model invariants, browser interaction, responsive layouts, source manifest hashes, asset links and graphic fit. Contract security and live chain activation are not reviewed in this task.
