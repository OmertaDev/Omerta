# Second campaign collection

Continue the existing ink, paper, brass and teal palette; reuse the Omerta display face and Arial text. Editorial layouts with ruled sections, visible numbered steps and one main interaction per section. No new generated assets or paid API requests.

The interactive demo has three independent, resettable simulations: fee allocation (validated amount and bounded surge), salvage/craft (inventory, one-time consumption and unmet inputs), and evidence (source independence, sharing, revoked access and completion). It never connects a wallet or calls the production game. Native buttons, labels and status regions provide keyboard and screen-reader feedback.

Audience sheets share a print layout, one useful promise, three concrete examples, release status and a specific call to action. Developer material combines exact observed API routes, a runnable in-memory fixture and a reused narrated clip. Announcements are drafts, grouped by audience and release milestone. The case study separates observed results from implications.

Desktop uses two columns for demonstration controls/results and illustrated articles. Below 760px all panels stack and buttons wrap. Controls have minimum 44px targets, visible focus, disabled explanations, and explicit validation. No asynchronous loading state is needed in the simulation; invalid inputs clear results. Native video provides loading/error behavior. Motion is unnecessary; inherited reduced-motion rules remain. Print hides navigation and interactive controls and uses white paper with dark text.

Verification: test money conservation and surge bounds, one-time salvage and craft consumption, independent origins/current-access gates; then verify browser interaction and narrow-screen overflow. Check exported PDFs fit one page, media decodes, and local links resolve.
